---
# Migration-friendly Installation CR.
# The operator auto-detects existing Calico settings from kube-system
# and migrates resources to calico-system namespace.
# For more information, see: https://docs.tigera.io/calico/3.31/operations/operator-migration
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  kubeletVolumePluginPath: None
  registry: {{ local_registry }}/quay.io
  imagePullSecrets:
    - name: {{ registry_local_secret_name | default('registry-local-secret') }}
